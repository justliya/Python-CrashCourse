# Storing numbers/integers as variables 

#You can only concatenate strings not integers stored in variables so you would need to convert the integer to a string or insert str before the variable in parenthesis

favorite_number = 19

message = ('My favorite number is ' + str(favorite_number))

print(message)